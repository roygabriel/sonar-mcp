# Risk Matrix

| Risk | Likelihood | Impact | Trigger | Mitigation | Gate | Owner |
|---|---|---|---|---|---|---|
| SonarQube REST API integration drift in search_issues tool (derived from sonarqube-client dep) | Medium - external API but version pinning used | High - blocks core read functionality | SonarQube API response shape changes or auth failures on projectKey/branch filters | Mirror k8s client error handling; add contract tests with real 9.8+ payloads via nock; enforce token redaction in logs; monitor OTEL traces for >5% error rate on /issues/search endpoint and alert via Prometheus | component-test-gate | SoftwareEngineer |
| Framework component divergence (OTEL, logging, healthchecks) from k8s reference impl | Low - shared modules available | High - breaks observability and gateway compatibility | Manual fork edits instead of importing mcp-framework package | Copy exact OTEL setup and redaction regex (sonar.*token) from reference; run differential test suite asserting identical trace spans and log shapes; include healthcheck e2e test; monitor trace sampling (target 20%) and redaction audit logs daily | phase-audit-2 | PlatformArchitect |
| transition_issue tool state machine errors with SonarQube workflow | Medium - SonarQube enforces strict transitions | High - can corrupt issue state in target projects | Invalid transition attempted (e.g. resolving closed issue) or 400 response | Pre-flight status lookup before transition; map only resolve/wontfix/falsepositive with explicit error handling; add 3x test cases per transition type against test project; surface specific error codes in structured logs; monitor transition failure rate >1% via OTEL metrics | tool-validation-gate | SoftwareEngineer |
| MCP gateway registration failure for new tools with incorrect metadata | Medium - schema generation is automated | Medium - impacts discoverability and risk classification | Mismatch between tool JSON schema and gateway expected format (read-only vs write) | Generate schemas directly from tool TypeScript interfaces; validate descriptions contain project/branch hints; unit test registration output against golden file from k8s impl; query /capabilities endpoint post-startup in CI; monitor gateway registration errors in structured logs | gateway-registration-gate | PlatformArchitect |
| Helm chart deployment and Vault secret injection failure for SonarQube creds | Medium - follows existing patterns but new secrets added | High - prevents dev environment rollout | Missing Vault annotations or config key drift in values.yaml | Reuse identical secret injection templates and sidecar from cruvero-mcp-k8s; add helm unittest covering sonar.url/token paths; deploy to ephemeral namespace in CI with vault mock; verify pod env vars and readiness probe; monitor deployment failures via ArgoCD alerts and pod init-container logs | helm-deploy-gate | SoftwareEngineer |
| Dependency coupling: sonarqube-client → config (risk: 0.2) | Low - config is simple key-value | Medium - affects all API calls | Config contract drift (missing url/token/timeout fields) | Add Zod schema validation on client init with exact fields from k8s reference; include config loading test with Vault mock responses; fail fast on missing required keys at startup; monitor config load errors in healthcheck and startup logs | dependency-gate | SoftwareEngineer |
| Dependency coupling: tools-issues → sonarqube-client (risk: 0.1) | Low - narrow interface | High - breaks both tools | Interface or return-type changes in client without tool updates | Enforce contract via integration tests using recorded SonarQube responses; use dependency injection for client instance; run full tool test suite on client changes; detect via CI test failures and runtime type errors in traces | dependency-gate | PlatformArchitect |
| Dependency coupling: helm-chart → config (risk: 0.3) | Medium - new SonarQube params added | High - blocks secret injection and deployment | Helm values/config template drift for SonarQube URL/token | Mirror configmap and secret templates exactly from k8s chart; add helm template tests asserting all new keys are rendered; validate Vault path in CI; monitor rendered manifests with conftest and deployment status in dev cluster | helm-lint-gate | SoftwareEngineer |
| Dependency coupling: gateway-registration → tools-issues (risk: 0.1) | Low - registration is declarative | Medium - tools not exposed correctly | Tool definitions or risk hints drift between modules | Export tool metadata object from tools-issues and import in registration; add assertion test verifying registered tools match exported schemas and classifications; run end-to-end gateway capability check; monitor for capability mismatch errors in gateway logs | registration-gate | PlatformArchitect |